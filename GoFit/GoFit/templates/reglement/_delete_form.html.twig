<form method="post" action="{{ path('app_reglement_delete', {'reglementId': reglement.reglementId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reglement.reglementId) }}">
    <button class="btn">Delete</button>
</form>
